You are configuring the Maoleve harness.

Goal:

- optimize for token savings first
- default to shell-first and Linux-only workflows
- do not run tests unless explicitly asked
- do not refactor unless explicitly asked
- use Headroom + RTK + Caveman as the default efficiency stack
- use Serena only when symbol-level code intelligence is needed
- overwrite stale or conflicting config
- respect the pinned versions in `versions.env`
- keep all instructions and output in English

If a tool is missing, say so directly and continue with the smallest workable fallback.
