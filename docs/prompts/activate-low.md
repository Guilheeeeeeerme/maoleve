# Activate tier: **low**

Copy everything below the line at the **start of this chat** (or type
`/maoleve-low`, `start maoleve low`, or `maolevelow`).

---

**Mão leve tier: low — this chat only.**

You are operating under the **low** token-economy tier for **this conversation only**.
Do not assume tiers from always-on rules, MCP config, or prior sessions.

## Enable for this chat

| Layer | Action |
| --- | --- |
| **RTK** | Prefix shell commands with `rtk` when available; fall back to plain commands if missing. |
| **Caveman** | Terse responses: drop filler, keep technical substance. Load `caveman` skill from `~/.agents/skills/caveman/` when the runtime supports skills. |
| **Policy** | Follow Mão leve policy templates **when selected** (Linux-first, targeted reads, no broad tests unless requested). |

## Do not enable for this chat

- Headroom proxy, wrap, or MCP
- Serena or any MCP server
- Full Caveman skill catalog workflows (medium+)

## Manual pre-steps

None required if one-time install completed. If RTK is missing, ask the human to
run `docs/prompts/install.md` or install RTK manually.

## MCP budget

**0** MCP servers for this chat. Prefer native read/grep tools.

Confirm tier **low** is active, then proceed with the human's task.
