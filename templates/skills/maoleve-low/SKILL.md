---
name: maoleve-low
description: Activate Mão leve low for this chat. Matches /maoleve-low, start maoleve low, maolevelow, or a bare maoleve only when no tier is named.
disable-model-invocation: true
---

Activate Mão leve **low** for this chat only. Confirm that low is active, then
continue with the user's task.

- Prefix shell commands with `rtk` when available; fall back to plain commands.
- Use terse, technically complete responses (Caveman).
- Follow the Mão leve policy only for this chat.
- Do not enable Headroom proxy or MCP, Serena, or any other MCP.
- Do not run broad tests or refactors unless requested; start with targeted reads.

This is per-chat state. Do not infer it from prior chats or always-on config.
